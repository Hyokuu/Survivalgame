/// @description Insert description here
// You can write your code in this editor

if (mouse_check_button_pressed(mb_left) && instance_position(mouse_x, mouse_y, oGarrison)) {
    show_debug_message("Clicked on oGarrison. Attempting to open menu.");

    // Set the menu_open variable to true
    oGarrisonMenu.menu_open = true;

    // Set the menu position to a fixed location (e.g., room center)

}


